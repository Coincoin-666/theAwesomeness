let text = document.getElementById("text");

// window.onscroll = function biggerTxt() {
//     if (document.body.scrollTop > 50 || document.documentElement.scrollTop > 50) {
//         text.className = "bigBody";
//     } else {
//         text.className = "bodyCurrent";
//     };
// };

let textSize = document.getElementById("text").style.fontSize;

function biggerTheBetter() {
    for (let textSize = 0; textSize < 120; textSize++) {
        const element = array[textSize];
        
    }
}

// window.addEventListener('scroll', () => {
//     const {scrollHeight, scrollTop, clientHeight} = document.documentElement;
//     if (scrollTop + clientHeight > scrollHeight - 5) {
//         setTimeout();
//         while (textSize > 120) {
//             biggerTheBetter();
//         }
//     }
// });




// window.addEventListener('scroll', () => {
// 	const {scrollHeight, scrollTop,clientHeight} = document.documentElement;
// 	if(scrollTop + clientHeight > scrollHeight - 5){
// 		setTimeout(createPost,2000);